naftiko: 1.0.0-alpha2
info:
  label: Atlassian Bit Bucket Workspaces API — Deployments
  description: 'Atlassian Bit Bucket Workspaces API — Deployments. 1 operations. Lead operation: Atlassian Delete a Deploy
    Key From a Project. Self-contained Naftiko capability covering one Atlassian business surface.'
  tags:
  - Atlassian
  - Deployments
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ATLASSIAN_API_KEY: ATLASSIAN_API_KEY
capability:
  consumes:
  - type: http
    namespace: bitbucket-workspaces-deployments
    baseUri: https://api.bitbucket.org/2.0
    description: Atlassian Bit Bucket Workspaces API — Deployments business capability. Self-contained, no shared references.
    resources:
    - name: workspaces-workspace-projects-project_key-deploy-keys-key_id
      path: /workspaces/{workspace}/projects/{project_key}/deploy-keys/{key_id}
      operations:
      - name: atlassiandeleteadeploykeyfromaproject
        method: DELETE
        description: Atlassian Delete a Deploy Key From a Project
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: bitbucket-workspaces-deployments-rest
    port: 8080
    description: REST adapter for Atlassian Bit Bucket Workspaces API — Deployments. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/workspaces/{workspace}/projects/{project-key}/deploy-keys/{key-id}
      name: workspaces-workspace-projects-project-key-deploy-keys-key-id
      description: REST surface for workspaces-workspace-projects-project_key-deploy-keys-key_id.
      operations:
      - method: DELETE
        name: atlassiandeleteadeploykeyfromaproject
        description: Atlassian Delete a Deploy Key From a Project
        call: bitbucket-workspaces-deployments.atlassiandeleteadeploykeyfromaproject
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: bitbucket-workspaces-deployments-mcp
    port: 9090
    transport: http
    description: MCP adapter for Atlassian Bit Bucket Workspaces API — Deployments. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: atlassian-delete-deploy-key-project
      description: Atlassian Delete a Deploy Key From a Project
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: bitbucket-workspaces-deployments.atlassiandeleteadeploykeyfromaproject
      outputParameters:
      - type: object
        mapping: $.
