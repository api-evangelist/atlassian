naftiko: 1.0.0-alpha2
info:
  label: Atlassian Bit Bucket Workspaces API — Add
  description: 'Atlassian Bit Bucket Workspaces API — Add. 1 operations. Lead operation: Atlassian Add the Specific User as
    a Default Reviewer for the Project. Self-contained Naftiko capability covering one Atlassian business surface.'
  tags:
  - Atlassian
  - Add
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ATLASSIAN_API_KEY: ATLASSIAN_API_KEY
capability:
  consumes:
  - type: http
    namespace: bitbucket-workspaces-add
    baseUri: https://api.bitbucket.org/2.0
    description: Atlassian Bit Bucket Workspaces API — Add business capability. Self-contained, no shared references.
    resources:
    - name: workspaces-workspace-projects-project_key-default-reviewers-selected_user
      path: /workspaces/{workspace}/projects/{project_key}/default-reviewers/{selected_user}
      operations:
      - name: atlassianaddthespecificuserasadefaultreviewerfortheproject
        method: PUT
        description: Atlassian Add the Specific User as a Default Reviewer for the Project
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: bitbucket-workspaces-add-rest
    port: 8080
    description: REST adapter for Atlassian Bit Bucket Workspaces API — Add. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/workspaces/{workspace}/projects/{project-key}/default-reviewers/{selected-user}
      name: workspaces-workspace-projects-project-key-default-reviewers-selected-user
      description: REST surface for workspaces-workspace-projects-project_key-default-reviewers-selected_user.
      operations:
      - method: PUT
        name: atlassianaddthespecificuserasadefaultreviewerfortheproject
        description: Atlassian Add the Specific User as a Default Reviewer for the Project
        call: bitbucket-workspaces-add.atlassianaddthespecificuserasadefaultreviewerfortheproject
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: bitbucket-workspaces-add-mcp
    port: 9090
    transport: http
    description: MCP adapter for Atlassian Bit Bucket Workspaces API — Add. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: atlassian-add-specific-user-default
      description: Atlassian Add the Specific User as a Default Reviewer for the Project
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: bitbucket-workspaces-add.atlassianaddthespecificuserasadefaultreviewerfortheproject
      outputParameters:
      - type: object
        mapping: $.
