naftiko: 1.0.0-alpha2
info:
  label: Atlassian Bit Bucket Addon API — Delete
  description: 'Atlassian Bit Bucket Addon API — Delete. 2 operations. Lead operation: Atlassian Delete Addon Linkers Key
    Values. Self-contained Naftiko capability covering one Atlassian business surface.'
  tags:
  - Atlassian
  - Delete
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ATLASSIAN_API_KEY: ATLASSIAN_API_KEY
capability:
  consumes:
  - type: http
    namespace: bitbucket-addon-delete
    baseUri: https://api.bitbucket.org/2.0
    description: Atlassian Bit Bucket Addon API — Delete business capability. Self-contained, no shared references.
    resources:
    - name: addon-linkers-linker_key-values
      path: /addon/linkers/{linker_key}/values
      operations:
      - name: deleteaddonlinkerslinkerkeyvalues
        method: DELETE
        description: Atlassian Delete Addon Linkers Key Values
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: addon-linkers-linker_key-values-value_id
      path: /addon/linkers/{linker_key}/values/{value_id}
      operations:
      - name: deleteaddonlinkerslinkerkeyvaluesvalueid
        method: DELETE
        description: Atlassian Delete Addon Linkers Key Values Value Id
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: bitbucket-addon-delete-rest
    port: 8080
    description: REST adapter for Atlassian Bit Bucket Addon API — Delete. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/addon/linkers/{linker-key}/values
      name: addon-linkers-linker-key-values
      description: REST surface for addon-linkers-linker_key-values.
      operations:
      - method: DELETE
        name: deleteaddonlinkerslinkerkeyvalues
        description: Atlassian Delete Addon Linkers Key Values
        call: bitbucket-addon-delete.deleteaddonlinkerslinkerkeyvalues
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/addon/linkers/{linker-key}/values/{value-id}
      name: addon-linkers-linker-key-values-value-id
      description: REST surface for addon-linkers-linker_key-values-value_id.
      operations:
      - method: DELETE
        name: deleteaddonlinkerslinkerkeyvaluesvalueid
        description: Atlassian Delete Addon Linkers Key Values Value Id
        call: bitbucket-addon-delete.deleteaddonlinkerslinkerkeyvaluesvalueid
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: bitbucket-addon-delete-mcp
    port: 9090
    transport: http
    description: MCP adapter for Atlassian Bit Bucket Addon API — Delete. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: atlassian-delete-addon-linkers-key
      description: Atlassian Delete Addon Linkers Key Values
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: bitbucket-addon-delete.deleteaddonlinkerslinkerkeyvalues
      outputParameters:
      - type: object
        mapping: $.
    - name: atlassian-delete-addon-linkers-key-2
      description: Atlassian Delete Addon Linkers Key Values Value Id
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: bitbucket-addon-delete.deleteaddonlinkerslinkerkeyvaluesvalueid
      outputParameters:
      - type: object
        mapping: $.
