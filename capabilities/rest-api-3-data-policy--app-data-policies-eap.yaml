naftiko: 1.0.0-alpha2
info:
  label: Atlassian rest/api/3/data-policy/ — App Data Policies (EAP)
  description: 'Atlassian rest/api/3/data-policy/ — App Data Policies (EAP). 1 operations. Lead operation: Atlassian Get Data
    Policy For Projects Eap. Self-contained Naftiko capability covering one Atlassian business surface.'
  tags:
  - Atlassian
  - App Data Policies (EAP)
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ATLASSIAN_API_KEY: ATLASSIAN_API_KEY
capability:
  consumes:
  - type: http
    namespace: rest-api-3-data-policy--app-data-policies-eap
    baseUri: https://your-domain.atlassian.net
    description: Atlassian rest/api/3/data-policy/ — App Data Policies (EAP) business capability. Self-contained, no shared
      references.
    resources:
    - name: rest-api-3-data-policy-project
      path: /rest/api/3/data-policy/project
      operations:
      - name: atlassiangetpolicies
        method: GET
        description: Atlassian Get Data Policy For Projects Eap
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: ids
          in: query
          type: string
          description: A list of project identifiers. This parameter accepts a comma-separated list.
  exposes:
  - type: rest
    namespace: rest-api-3-data-policy--app-data-policies-eap-rest
    port: 8080
    description: REST adapter for Atlassian rest/api/3/data-policy/ — App Data Policies (EAP). One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/api/3/data-policy/project
      name: rest-api-3-data-policy-project
      description: REST surface for rest-api-3-data-policy-project.
      operations:
      - method: GET
        name: atlassiangetpolicies
        description: Atlassian Get Data Policy For Projects Eap
        call: rest-api-3-data-policy--app-data-policies-eap.atlassiangetpolicies
        with:
          ids: rest.ids
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: rest-api-3-data-policy--app-data-policies-eap-mcp
    port: 9090
    transport: http
    description: MCP adapter for Atlassian rest/api/3/data-policy/ — App Data Policies (EAP). One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: atlassian-get-data-policy-projects
      description: Atlassian Get Data Policy For Projects Eap
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: rest-api-3-data-policy--app-data-policies-eap.atlassiangetpolicies
      with:
        ids: tools.ids
      outputParameters:
      - type: object
        mapping: $.
