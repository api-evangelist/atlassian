naftiko: 1.0.0-alpha2
info:
  label: Atlassian Bit Bucket Workspaces API — Remove
  description: 'Atlassian Bit Bucket Workspaces API — Remove. 1 operations. Lead operation: Atlassian Remove the Specific
    User From the Project S Default Reviewers. Self-contained Naftiko capability covering one Atlassian business surface.'
  tags:
  - Atlassian
  - Remove
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ATLASSIAN_API_KEY: ATLASSIAN_API_KEY
capability:
  consumes:
  - type: http
    namespace: bitbucket-workspaces-remove
    baseUri: https://api.bitbucket.org/2.0
    description: Atlassian Bit Bucket Workspaces API — Remove business capability. Self-contained, no shared references.
    resources:
    - name: workspaces-workspace-projects-project_key-default-reviewers-selected_user
      path: /workspaces/{workspace}/projects/{project_key}/default-reviewers/{selected_user}
      operations:
      - name: atlassianremovethespecificuserfromtheprojectsdefaultreviewers
        method: DELETE
        description: Atlassian Remove the Specific User From the Project S Default Reviewers
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: bitbucket-workspaces-remove-rest
    port: 8080
    description: REST adapter for Atlassian Bit Bucket Workspaces API — Remove. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/workspaces/{workspace}/projects/{project-key}/default-reviewers/{selected-user}
      name: workspaces-workspace-projects-project-key-default-reviewers-selected-user
      description: REST surface for workspaces-workspace-projects-project_key-default-reviewers-selected_user.
      operations:
      - method: DELETE
        name: atlassianremovethespecificuserfromtheprojectsdefaultreviewers
        description: Atlassian Remove the Specific User From the Project S Default Reviewers
        call: bitbucket-workspaces-remove.atlassianremovethespecificuserfromtheprojectsdefaultreviewers
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: bitbucket-workspaces-remove-mcp
    port: 9090
    transport: http
    description: MCP adapter for Atlassian Bit Bucket Workspaces API — Remove. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: atlassian-remove-specific-user-project
      description: Atlassian Remove the Specific User From the Project S Default Reviewers
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: bitbucket-workspaces-remove.atlassianremovethespecificuserfromtheprojectsdefaultreviewers
      outputParameters:
      - type: object
        mapping: $.
