naftiko: 1.0.0-alpha2
info:
  label: Atlassian rest/api/3/customFieldOption/ — Issue Custom Field Options
  description: 'Atlassian rest/api/3/customFieldOption/ — Issue Custom Field Options. 1 operations. Lead operation: Atlassian
    Get Custom Field Option. Self-contained Naftiko capability covering one Atlassian business surface.'
  tags:
  - Atlassian
  - Issue Custom Field Options
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ATLASSIAN_API_KEY: ATLASSIAN_API_KEY
capability:
  consumes:
  - type: http
    namespace: rest-api-3-customfieldoption--issue-custom-field-options
    baseUri: https://your-domain.atlassian.net
    description: Atlassian rest/api/3/customFieldOption/ — Issue Custom Field Options business capability. Self-contained,
      no shared references.
    resources:
    - name: rest-api-3-customFieldOption-id
      path: /rest/api/3/customFieldOption/{id}
      operations:
      - name: atlassiangetcustomfieldoption
        method: GET
        description: Atlassian Get Custom Field Option
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: id
          in: path
          type: string
          description: The ID of the custom field option.
          required: true
  exposes:
  - type: rest
    namespace: rest-api-3-customfieldoption--issue-custom-field-options-rest
    port: 8080
    description: REST adapter for Atlassian rest/api/3/customFieldOption/ — Issue Custom Field Options. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/api/3/customfieldoption/{id}
      name: rest-api-3-customfieldoption-id
      description: REST surface for rest-api-3-customFieldOption-id.
      operations:
      - method: GET
        name: atlassiangetcustomfieldoption
        description: Atlassian Get Custom Field Option
        call: rest-api-3-customfieldoption--issue-custom-field-options.atlassiangetcustomfieldoption
        with:
          id: rest.id
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: rest-api-3-customfieldoption--issue-custom-field-options-mcp
    port: 9090
    transport: http
    description: MCP adapter for Atlassian rest/api/3/customFieldOption/ — Issue Custom Field Options. One tool per consumed
      operation, routed inline through this capability's consumes block.
    tools:
    - name: atlassian-get-custom-field-option
      description: Atlassian Get Custom Field Option
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: rest-api-3-customfieldoption--issue-custom-field-options.atlassiangetcustomfieldoption
      with:
        id: tools.id
      outputParameters:
      - type: object
        mapping: $.
