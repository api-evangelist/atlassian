naftiko: 1.0.0-alpha2
info:
  label: Atlassian Bit Bucket Workspaces API — Repositories
  description: 'Atlassian Bit Bucket Workspaces API — Repositories. 2 operations. Lead operation: Atlassian List All Repository
    Permissions For A Workspace. Self-contained Naftiko capability covering one Atlassian business surface.'
  tags:
  - Atlassian
  - Repositories
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ATLASSIAN_API_KEY: ATLASSIAN_API_KEY
capability:
  consumes:
  - type: http
    namespace: rest-api-3-announcementbanner--repositories
    baseUri: https://api.bitbucket.org/2.0
    description: Atlassian Bit Bucket Workspaces API — Repositories business capability. Self-contained, no shared references.
    resources:
    - name: workspaces-workspace-permissions-repositories
      path: /workspaces/{workspace}/permissions/repositories
      operations:
      - name: atlassianlistallrepositorypermissionsforaworkspace
        method: GET
        description: Atlassian List All Repository Permissions For A Workspace
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: q
          in: query
          type: string
          description: Query string to narrow down the response as per
        - name: sort
          in: query
          type: string
          description: Name of a response property sort the result by as per
    - name: workspaces-workspace-permissions-repositories-repo_slug
      path: /workspaces/{workspace}/permissions/repositories/{repo_slug}
      operations:
      - name: atlassianlistarepositorypermissionsforaworkspace
        method: GET
        description: Atlassian List A Repository Permissions For A Workspace
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: q
          in: query
          type: string
          description: Query string to narrow down the response as per
        - name: sort
          in: query
          type: string
          description: Name of a response property sort the result by as per
  exposes:
  - type: rest
    namespace: rest-api-3-announcementbanner--repositories-rest
    port: 8080
    description: REST adapter for Atlassian Bit Bucket Workspaces API — Repositories. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/workspaces/{workspace}/permissions/repositories
      name: workspaces-workspace-permissions-repositories
      description: REST surface for workspaces-workspace-permissions-repositories.
      operations:
      - method: GET
        name: atlassianlistallrepositorypermissionsforaworkspace
        description: Atlassian List All Repository Permissions For A Workspace
        call: rest-api-3-announcementbanner--repositories.atlassianlistallrepositorypermissionsforaworkspace
        with:
          q: rest.q
          sort: rest.sort
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/workspaces/{workspace}/permissions/repositories/{repo-slug}
      name: workspaces-workspace-permissions-repositories-repo-slug
      description: REST surface for workspaces-workspace-permissions-repositories-repo_slug.
      operations:
      - method: GET
        name: atlassianlistarepositorypermissionsforaworkspace
        description: Atlassian List A Repository Permissions For A Workspace
        call: rest-api-3-announcementbanner--repositories.atlassianlistarepositorypermissionsforaworkspace
        with:
          q: rest.q
          sort: rest.sort
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: rest-api-3-announcementbanner--repositories-mcp
    port: 9090
    transport: http
    description: MCP adapter for Atlassian Bit Bucket Workspaces API — Repositories. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: atlassian-list-all-repository-permissions
      description: Atlassian List All Repository Permissions For A Workspace
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: rest-api-3-announcementbanner--repositories.atlassianlistallrepositorypermissionsforaworkspace
      with:
        q: tools.q
        sort: tools.sort
      outputParameters:
      - type: object
        mapping: $.
    - name: atlassian-list-repository-permissions-workspace
      description: Atlassian List A Repository Permissions For A Workspace
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: rest-api-3-announcementbanner--repositories.atlassianlistarepositorypermissionsforaworkspace
      with:
        q: tools.q
        sort: tools.sort
      outputParameters:
      - type: object
        mapping: $.
