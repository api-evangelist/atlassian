naftiko: 1.0.0-alpha2
info:
  label: Atlassian rest/api/3/securitylevel/ — Issue Security Level
  description: 'Atlassian rest/api/3/securitylevel/ — Issue Security Level. 1 operations. Lead operation: Atlassian Get Issue
    Security Level. Self-contained Naftiko capability covering one Atlassian business surface.'
  tags:
  - Atlassian
  - Issue Security Level
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ATLASSIAN_API_KEY: ATLASSIAN_API_KEY
capability:
  consumes:
  - type: http
    namespace: rest-api-3-securitylevel--issue-security-level
    baseUri: https://your-domain.atlassian.net
    description: Atlassian rest/api/3/securitylevel/ — Issue Security Level business capability. Self-contained, no shared
      references.
    resources:
    - name: rest-api-3-securitylevel-id
      path: /rest/api/3/securitylevel/{id}
      operations:
      - name: atlassiangetissuesecuritylevel
        method: GET
        description: Atlassian Get Issue Security Level
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: id
          in: path
          type: string
          description: The ID of the issue security level.
          required: true
  exposes:
  - type: rest
    namespace: rest-api-3-securitylevel--issue-security-level-rest
    port: 8080
    description: REST adapter for Atlassian rest/api/3/securitylevel/ — Issue Security Level. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/api/3/securitylevel/{id}
      name: rest-api-3-securitylevel-id
      description: REST surface for rest-api-3-securitylevel-id.
      operations:
      - method: GET
        name: atlassiangetissuesecuritylevel
        description: Atlassian Get Issue Security Level
        call: rest-api-3-securitylevel--issue-security-level.atlassiangetissuesecuritylevel
        with:
          id: rest.id
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: rest-api-3-securitylevel--issue-security-level-mcp
    port: 9090
    transport: http
    description: MCP adapter for Atlassian rest/api/3/securitylevel/ — Issue Security Level. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: atlassian-get-issue-security-level
      description: Atlassian Get Issue Security Level
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: rest-api-3-securitylevel--issue-security-level.atlassiangetissuesecuritylevel
      with:
        id: tools.id
      outputParameters:
      - type: object
        mapping: $.
