naftiko: 1.0.0-alpha2
info:
  label: Atlassian Confluence Content API — Type
  description: 'Atlassian Confluence Content API — Type. 1 operations. Lead operation: Atlassian Get Content Children by Type.
    Self-contained Naftiko capability covering one Atlassian business surface.'
  tags:
  - Atlassian
  - Type
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ATLASSIAN_API_KEY: ATLASSIAN_API_KEY
capability:
  consumes:
  - type: http
    namespace: confluence-content-type
    baseUri: ''
    description: Atlassian Confluence Content API — Type business capability. Self-contained, no shared references.
    resources:
    - name: wiki-rest-api-content-id-child-type
      path: /wiki/rest/api/content/{id}/child/{type}
      operations:
      - name: getcontentchildrenbytype
        method: GET
        description: Atlassian Get Content Children by Type
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: id
          in: path
          type: string
          description: The ID of the content to be queried for its children.
          required: true
        - name: type
          in: path
          type: string
          description: The type of children to return.
          required: true
        - name: parentVersion
          in: query
          type: integer
          description: The version of the parent content to retrieve children for.
        - name: start
          in: query
          type: integer
          description: The starting index of the returned content.
        - name: limit
          in: query
          type: integer
          description: The maximum number of content to return per page. Note,
    authentication:
      type: bearer
      token: '{{env.ATLASSIAN_API_KEY}}'
  exposes:
  - type: rest
    namespace: confluence-content-type-rest
    port: 8080
    description: REST adapter for Atlassian Confluence Content API — Type. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/wiki/rest/api/content/{id}/child/{type}
      name: wiki-rest-api-content-id-child-type
      description: REST surface for wiki-rest-api-content-id-child-type.
      operations:
      - method: GET
        name: getcontentchildrenbytype
        description: Atlassian Get Content Children by Type
        call: confluence-content-type.getcontentchildrenbytype
        with:
          id: rest.id
          type: rest.type
          parentVersion: rest.parentVersion
          start: rest.start
          limit: rest.limit
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: confluence-content-type-mcp
    port: 9090
    transport: http
    description: MCP adapter for Atlassian Confluence Content API — Type. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: atlassian-get-content-children-type
      description: Atlassian Get Content Children by Type
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: confluence-content-type.getcontentchildrenbytype
      with:
        id: tools.id
        type: tools.type
        parentVersion: tools.parentVersion
        start: tools.start
        limit: tools.limit
      outputParameters:
      - type: object
        mapping: $.
