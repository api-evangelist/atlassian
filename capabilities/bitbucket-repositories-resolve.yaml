naftiko: 1.0.0-alpha2
info:
  label: Atlassian Bit Bucket Repositories API — Resolve
  description: 'Atlassian Bit Bucket Repositories API — Resolve. 1 operations. Lead operation: Atlassian Resolve Comment Thread.
    Self-contained Naftiko capability covering one Atlassian business surface.'
  tags:
  - Atlassian
  - Resolve
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ATLASSIAN_API_KEY: ATLASSIAN_API_KEY
capability:
  consumes:
  - type: http
    namespace: bitbucket-repositories-resolve
    baseUri: https://api.bitbucket.org/2.0
    description: Atlassian Bit Bucket Repositories API — Resolve business capability. Self-contained, no shared references.
    resources:
    - name: repositories-workspace-repo_slug-pullrequests-pull_request_id-comments-comment_i
      path: /repositories/{workspace}/{repo_slug}/pullrequests/{pull_request_id}/comments/{comment_id}/resolve
      operations:
      - name: resolvecommentthread
        method: POST
        description: Atlassian Resolve Comment Thread
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.ATLASSIAN_API_KEY}}'
  exposes:
  - type: rest
    namespace: bitbucket-repositories-resolve-rest
    port: 8080
    description: REST adapter for Atlassian Bit Bucket Repositories API — Resolve. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/repositories/{workspace}/{repo-slug}/pullrequests/{pull-request-id}/comments/{comment-id}/resolve
      name: repositories-workspace-repo-slug-pullrequests-pull-request-id-comments-comment-i
      description: REST surface for repositories-workspace-repo_slug-pullrequests-pull_request_id-comments-comment_i.
      operations:
      - method: POST
        name: resolvecommentthread
        description: Atlassian Resolve Comment Thread
        call: bitbucket-repositories-resolve.resolvecommentthread
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: bitbucket-repositories-resolve-mcp
    port: 9090
    transport: http
    description: MCP adapter for Atlassian Bit Bucket Repositories API — Resolve. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: atlassian-resolve-comment-thread
      description: Atlassian Resolve Comment Thread
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: bitbucket-repositories-resolve.resolvecommentthread
      outputParameters:
      - type: object
        mapping: $.
