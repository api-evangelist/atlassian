naftiko: 1.0.0-alpha2
info:
  label: Atlassian The Jira Cloud platform REST API — Issue Security Level
  description: 'Atlassian The Jira Cloud platform REST API — Issue Security Level. 2 operations. Lead operation: Atlassian
    Get Issue Security Level Members By Issue Security Scheme. Self-contained Naftiko capability covering one Atlassian business
    surface.'
  tags:
  - Atlassian
  - Issue Security Level
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ATLASSIAN_API_KEY: ATLASSIAN_API_KEY
capability:
  consumes:
  - type: http
    namespace: jira-issue-security-level
    baseUri: https://your-domain.atlassian.net
    description: Atlassian The Jira Cloud platform REST API — Issue Security Level business capability. Self-contained, no
      shared references.
    resources:
    - name: rest-api-3-issuesecurityschemes-issueSecuritySchemeId-members
      path: /rest/api/3/issuesecurityschemes/{issueSecuritySchemeId}/members
      operations:
      - name: atlassiangetissuesecuritylevelmembers
        method: GET
        description: Atlassian Get Issue Security Level Members By Issue Security Scheme
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: issueSecuritySchemeId
          in: path
          type: integer
          description: The ID of the issue security scheme. Use the [Get issue security schemes](#api-rest-api-3-issuesecurityschemes-get)
            operation to get a list of issue security sc
          required: true
        - name: startAt
          in: query
          type: integer
          description: The index of the first item to return in a page of results (page offset).
        - name: maxResults
          in: query
          type: integer
          description: The maximum number of items to return per page.
        - name: issueSecurityLevelId
          in: query
          type: array
          description: 'The list of issue security level IDs. To include multiple issue security levels separate IDs with
            ampersand: `issueSecurityLevelId=10000&issueSecurityLevelId=10'
        - name: expand
          in: query
          type: string
          description: 'Use expand to include additional information in the response. This parameter accepts a comma-separated
            list. Expand options include:'
    - name: rest-api-3-securitylevel-id
      path: /rest/api/3/securitylevel/{id}
      operations:
      - name: atlassiangetissuesecuritylevel
        method: GET
        description: Atlassian Get Issue Security Level
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: id
          in: path
          type: string
          description: The ID of the issue security level.
          required: true
    authentication:
      type: bearer
      token: '{{env.ATLASSIAN_API_KEY}}'
  exposes:
  - type: rest
    namespace: jira-issue-security-level-rest
    port: 8080
    description: REST adapter for Atlassian The Jira Cloud platform REST API — Issue Security Level. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/api/3/issuesecurityschemes/{issuesecurityschemeid}/members
      name: rest-api-3-issuesecurityschemes-issuesecurityschemeid-members
      description: REST surface for rest-api-3-issuesecurityschemes-issueSecuritySchemeId-members.
      operations:
      - method: GET
        name: atlassiangetissuesecuritylevelmembers
        description: Atlassian Get Issue Security Level Members By Issue Security Scheme
        call: jira-issue-security-level.atlassiangetissuesecuritylevelmembers
        with:
          issueSecuritySchemeId: rest.issueSecuritySchemeId
          startAt: rest.startAt
          maxResults: rest.maxResults
          issueSecurityLevelId: rest.issueSecurityLevelId
          expand: rest.expand
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/api/3/securitylevel/{id}
      name: rest-api-3-securitylevel-id
      description: REST surface for rest-api-3-securitylevel-id.
      operations:
      - method: GET
        name: atlassiangetissuesecuritylevel
        description: Atlassian Get Issue Security Level
        call: jira-issue-security-level.atlassiangetissuesecuritylevel
        with:
          id: rest.id
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: jira-issue-security-level-mcp
    port: 9090
    transport: http
    description: MCP adapter for Atlassian The Jira Cloud platform REST API — Issue Security Level. One tool per consumed
      operation, routed inline through this capability's consumes block.
    tools:
    - name: atlassian-get-issue-security-level
      description: Atlassian Get Issue Security Level Members By Issue Security Scheme
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: jira-issue-security-level.atlassiangetissuesecuritylevelmembers
      with:
        issueSecuritySchemeId: tools.issueSecuritySchemeId
        startAt: tools.startAt
        maxResults: tools.maxResults
        issueSecurityLevelId: tools.issueSecurityLevelId
        expand: tools.expand
      outputParameters:
      - type: object
        mapping: $.
    - name: atlassian-get-issue-security-level-2
      description: Atlassian Get Issue Security Level
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: jira-issue-security-level.atlassiangetissuesecuritylevel
      with:
        id: tools.id
      outputParameters:
      - type: object
        mapping: $.
