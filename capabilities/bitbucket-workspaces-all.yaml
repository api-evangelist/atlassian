naftiko: 1.0.0-alpha2
info:
  label: Atlassian Bit Bucket Workspaces API — All
  description: 'Atlassian Bit Bucket Workspaces API — All. 1 operations. Lead operation: Atlassian List All Repository Permissions
    for a Workspace. Self-contained Naftiko capability covering one Atlassian business surface.'
  tags:
  - Atlassian
  - All
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ATLASSIAN_API_KEY: ATLASSIAN_API_KEY
capability:
  consumes:
  - type: http
    namespace: bitbucket-workspaces-all
    baseUri: https://api.bitbucket.org/2.0
    description: Atlassian Bit Bucket Workspaces API — All business capability. Self-contained, no shared references.
    resources:
    - name: workspaces-workspace-permissions-repositories
      path: /workspaces/{workspace}/permissions/repositories
      operations:
      - name: atlassianlistallrepositorypermissionsforaworkspace
        method: GET
        description: Atlassian List All Repository Permissions for a Workspace
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: q
          in: query
          type: string
          description: Query string to narrow down the response as per
        - name: sort
          in: query
          type: string
          description: Name of a response property sort the result by as per
  exposes:
  - type: rest
    namespace: bitbucket-workspaces-all-rest
    port: 8080
    description: REST adapter for Atlassian Bit Bucket Workspaces API — All. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/workspaces/{workspace}/permissions/repositories
      name: workspaces-workspace-permissions-repositories
      description: REST surface for workspaces-workspace-permissions-repositories.
      operations:
      - method: GET
        name: atlassianlistallrepositorypermissionsforaworkspace
        description: Atlassian List All Repository Permissions for a Workspace
        call: bitbucket-workspaces-all.atlassianlistallrepositorypermissionsforaworkspace
        with:
          q: rest.q
          sort: rest.sort
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: bitbucket-workspaces-all-mcp
    port: 9090
    transport: http
    description: MCP adapter for Atlassian Bit Bucket Workspaces API — All. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: atlassian-list-all-repository-permissions
      description: Atlassian List All Repository Permissions for a Workspace
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: bitbucket-workspaces-all.atlassianlistallrepositorypermissionsforaworkspace
      with:
        q: tools.q
        sort: tools.sort
      outputParameters:
      - type: object
        mapping: $.
