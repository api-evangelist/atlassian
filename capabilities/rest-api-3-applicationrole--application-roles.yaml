naftiko: 1.0.0-alpha2
info:
  label: Atlassian rest/api/3/applicationrole/ — Application Roles
  description: 'Atlassian rest/api/3/applicationrole/ — Application Roles. 1 operations. Lead operation: Atlassian Get Application
    Role. Self-contained Naftiko capability covering one Atlassian business surface.'
  tags:
  - Atlassian
  - Application Roles
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ATLASSIAN_API_KEY: ATLASSIAN_API_KEY
capability:
  consumes:
  - type: http
    namespace: rest-api-3-applicationrole--application-roles
    baseUri: https://your-domain.atlassian.net
    description: Atlassian rest/api/3/applicationrole/ — Application Roles business capability. Self-contained, no shared
      references.
    resources:
    - name: rest-api-3-applicationrole-key
      path: /rest/api/3/applicationrole/{key}
      operations:
      - name: atlassiangetapplicationrole
        method: GET
        description: Atlassian Get Application Role
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: key
          in: path
          type: string
          description: The key of the application role. Use the [Get all application roles](#api-rest-api-3-applicationrole-get)
            operation to get the key for each application role.
          required: true
  exposes:
  - type: rest
    namespace: rest-api-3-applicationrole--application-roles-rest
    port: 8080
    description: REST adapter for Atlassian rest/api/3/applicationrole/ — Application Roles. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/api/3/applicationrole/{key}
      name: rest-api-3-applicationrole-key
      description: REST surface for rest-api-3-applicationrole-key.
      operations:
      - method: GET
        name: atlassiangetapplicationrole
        description: Atlassian Get Application Role
        call: rest-api-3-applicationrole--application-roles.atlassiangetapplicationrole
        with:
          key: rest.key
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: rest-api-3-applicationrole--application-roles-mcp
    port: 9090
    transport: http
    description: MCP adapter for Atlassian rest/api/3/applicationrole/ — Application Roles. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: atlassian-get-application-role
      description: Atlassian Get Application Role
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: rest-api-3-applicationrole--application-roles.atlassiangetapplicationrole
      with:
        key: tools.key
      outputParameters:
      - type: object
        mapping: $.
