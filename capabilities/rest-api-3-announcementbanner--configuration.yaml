naftiko: 1.0.0-alpha2
info:
  label: Atlassian Bit Bucket Workspaces API — Configuration
  description: 'Atlassian Bit Bucket Workspaces API — Configuration. 1 operations. Lead operation: Atlassian Get Openid Configuration
    For Oidc In Pipelines. Self-contained Naftiko capability covering one Atlassian business surface.'
  tags:
  - Atlassian
  - Configuration
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ATLASSIAN_API_KEY: ATLASSIAN_API_KEY
capability:
  consumes:
  - type: http
    namespace: rest-api-3-announcementbanner--configuration
    baseUri: https://api.bitbucket.org/2.0
    description: Atlassian Bit Bucket Workspaces API — Configuration business capability. Self-contained, no shared references.
    resources:
    - name: workspaces-workspace-pipelines-config-identity-oidc-.well-known-openid-configura
      path: /workspaces/{workspace}/pipelines-config/identity/oidc/.well-known/openid-configuration
      operations:
      - name: atlassiangetoidcconfiguration
        method: GET
        description: Atlassian Get Openid Configuration For Oidc In Pipelines
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: workspace
          in: path
          type: string
          description: This can either be the workspace ID (slug) or the workspace UUID surrounded by curly-braces, for example
            `{workspace UUID}`.
          required: true
  exposes:
  - type: rest
    namespace: rest-api-3-announcementbanner--configuration-rest
    port: 8080
    description: REST adapter for Atlassian Bit Bucket Workspaces API — Configuration. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/workspaces/{workspace}/pipelines-config/identity/oidc/well-known/openid-configuration
      name: workspaces-workspace-pipelines-config-identity-oidc-well-known-openid-configura
      description: REST surface for workspaces-workspace-pipelines-config-identity-oidc-.well-known-openid-configura.
      operations:
      - method: GET
        name: atlassiangetoidcconfiguration
        description: Atlassian Get Openid Configuration For Oidc In Pipelines
        call: rest-api-3-announcementbanner--configuration.atlassiangetoidcconfiguration
        with:
          workspace: rest.workspace
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: rest-api-3-announcementbanner--configuration-mcp
    port: 9090
    transport: http
    description: MCP adapter for Atlassian Bit Bucket Workspaces API — Configuration. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: atlassian-get-openid-configuration-oidc
      description: Atlassian Get Openid Configuration For Oidc In Pipelines
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: rest-api-3-announcementbanner--configuration.atlassiangetoidcconfiguration
      with:
        workspace: tools.workspace
      outputParameters:
      - type: object
        mapping: $.
