naftiko: 1.0.0-alpha2
info:
  label: Atlassian Confluence Analytics API — Viewers
  description: 'Atlassian Confluence Analytics API — Viewers. 1 operations. Lead operation: Atlassian Get Viewers. Self-contained
    Naftiko capability covering one Atlassian business surface.'
  tags:
  - Atlassian
  - Viewers
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ATLASSIAN_API_KEY: ATLASSIAN_API_KEY
capability:
  consumes:
  - type: http
    namespace: confluence-analytics-viewers
    baseUri: ''
    description: Atlassian Confluence Analytics API — Viewers business capability. Self-contained, no shared references.
    resources:
    - name: wiki-rest-api-analytics-content-contentId-viewers
      path: /wiki/rest/api/analytics/content/{contentId}/viewers
      operations:
      - name: getviewers
        method: GET
        description: Atlassian Get Viewers
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: contentId
          in: path
          type: string
          description: The ID of the content to get the viewers for.
          required: true
        - name: fromDate
          in: query
          type: string
          description: The number of views for the content since the date.
  exposes:
  - type: rest
    namespace: confluence-analytics-viewers-rest
    port: 8080
    description: REST adapter for Atlassian Confluence Analytics API — Viewers. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/wiki/rest/api/analytics/content/{contentid}/viewers
      name: wiki-rest-api-analytics-content-contentid-viewers
      description: REST surface for wiki-rest-api-analytics-content-contentId-viewers.
      operations:
      - method: GET
        name: getviewers
        description: Atlassian Get Viewers
        call: confluence-analytics-viewers.getviewers
        with:
          contentId: rest.contentId
          fromDate: rest.fromDate
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: confluence-analytics-viewers-mcp
    port: 9090
    transport: http
    description: MCP adapter for Atlassian Confluence Analytics API — Viewers. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: atlassian-get-viewers
      description: Atlassian Get Viewers
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: confluence-analytics-viewers.getviewers
      with:
        contentId: tools.contentId
        fromDate: tools.fromDate
      outputParameters:
      - type: object
        mapping: $.
