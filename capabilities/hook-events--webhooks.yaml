naftiko: 1.0.0-alpha2
info:
  label: Atlassian hook events/ — Webhooks
  description: 'Atlassian hook events/ — Webhooks. 1 operations. Lead operation: Atlassian List Subscribable Webhook Types.
    Self-contained Naftiko capability covering one Atlassian business surface.'
  tags:
  - Atlassian
  - Webhooks
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ATLASSIAN_API_KEY: ATLASSIAN_API_KEY
capability:
  consumes:
  - type: http
    namespace: hook-events--webhooks
    baseUri: https://api.bitbucket.org/2.0
    description: Atlassian hook events/ — Webhooks business capability. Self-contained, no shared references.
    resources:
    - name: hook_events-subject_type
      path: /hook_events/{subject_type}
      operations:
      - name: atlassianlistsubscribablewebhooktypes
        method: GET
        description: Atlassian List Subscribable Webhook Types
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: hook-events--webhooks-rest
    port: 8080
    description: REST adapter for Atlassian hook events/ — Webhooks. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/hook-events/{subject-type}
      name: hook-events-subject-type
      description: REST surface for hook_events-subject_type.
      operations:
      - method: GET
        name: atlassianlistsubscribablewebhooktypes
        description: Atlassian List Subscribable Webhook Types
        call: hook-events--webhooks.atlassianlistsubscribablewebhooktypes
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: hook-events--webhooks-mcp
    port: 9090
    transport: http
    description: MCP adapter for Atlassian hook events/ — Webhooks. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: atlassian-list-subscribable-webhook-types
      description: Atlassian List Subscribable Webhook Types
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: hook-events--webhooks.atlassianlistsubscribablewebhooktypes
      outputParameters:
      - type: object
        mapping: $.
